apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: vote
  namespace: instavote
spec:
  minReplicas: 2
  maxReplicas: 4
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: vote
